{
  "name": "@one/memory-vector",
  "version": "0.2.0",
  "private": true,
  "description": "语义向量引擎适配层 — VectorStore 抽象、SQLite/sqlite-vec 实现、Embedding 模型接入",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
