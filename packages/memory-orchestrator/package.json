{
  "name": "@one/memory-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "混合查询编排器 — 向量粗召回 + 图精排序 + 融合打分",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc"
  },
  "dependencies": {
    "@one/memory-graph": "workspace:*",
    "@one/memory-vector": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
