{
  "name": "one-memory",
  "private": true,
  "workspaces": [
    "packages/memory-graph",
    "packages/memory-vector",
    "packages/memory-orchestrator",
    "packages/memory-mcp",
    "packages/memory-cli"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}